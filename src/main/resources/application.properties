# 设置端口号
server.port=8089

## 默认静态文件路径为resources/static，方入静态文件可直接访问。
## 也可以自定义静态文件路径。多个配置之间用“，”分割
spring.resources.static-locations=classpath:/image/,classpath:/static/,classpath:/common/

# 数据库配置
spring.datasource.url=jdbc:mysql://127.0.0.1:3306/test_springboot?useUnicode=true&characterEncoding=UTF-8&useSSL=false
spring.datasource.username=root
spring.datasource.password=9855286
spring.datasource.driver-class-name=com.mysql.jdbc.Driver
spring.datasource.max-active=20
spring.datasource.max-idle=8
spring.datasource.min-idle=8
spring.datasource.initial-size=10

